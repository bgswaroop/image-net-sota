import os

# Set up the directories for the current training / evaluation session
root_session_dir = os.path.dirname(os.path.abspath(__file__))  # This is the project root
session_dir = ""  # setting SESSION_DIR to "" (empty string), will create a new session directory

# Data source
# root_data_dir = r"D:\Data\imagenet-object-localization-challenge"
root_data_dir = r"D:\Data\imagenet-sample"

# Training configuration

